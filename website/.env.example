# Local development: create a random seed of 32+ characters, then npm run demo:accounts
DEMO_PASSWORD_SEED=
TURSO_DATABASE_URL=file:.data/unit-toko.db
# Vercel requires remote libsql URL, database token, and a Private Blob store.
TURSO_AUTH_TOKEN=
BLOB_READ_WRITE_TOKEN=
NEXT_PUBLIC_APP_URL=http://localhost:3000
